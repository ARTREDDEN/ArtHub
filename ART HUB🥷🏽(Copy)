--// Game Load Check
if not game:IsLoaded() then game.Loaded:Wait() end

--// --- ระบบกันขโมย (Anti-Leak System) ---
local CoreGui = game:GetService("CoreGui")
local OWNER_ID = 000000000 --// <--- เปลี่ยนเป็น ID ของมึงตรงนี้!

local function ShowAntiLeakScreen()
    local Screen = Instance.new("ScreenGui", CoreGui)
    Screen.Name = "AntiLeak"
    local Frame = Instance.new("ImageLabel", Screen)
    Frame.Size = UDim2.new(1, 0, 1, 0)
    Frame.Image = "rbxassetid://81609873060038" 
    Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Frame.ZIndex = 999
end

if game:GetService("Players").LocalPlayer.UserId ~= OWNER_ID then
    ShowAntiLeakScreen()
    return --// ถ้าไม่ใช่เจ้าของ สคริปต์จะหยุดทำงานที่ตรงนี้
end
--// --------------------------------------

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local camera = game.Workspace.CurrentCamera
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LOGO_ID = "rbxassetid://81609873060038"

local Settings = {
    Aimbot = false, Camlock = false, WallHack = false, 
    InfJump = false, Noclip = false, WallCheck = true,
    TeamCheck = true, Smoothness = 0.2, LockPart = "Head", CurrentTarget = nil 
}

RunService.Stepped:Connect(function()
    if Settings.Noclip and LP.Character then
        for _, v in pairs(LP.Character:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
    end
end)

local aimRadius = 150
local fovCircle = Drawing.new("Circle")
fovCircle.Visible = false
fovCircle.Color = Color3.new(1, 0, 0)
fovCircle.Thickness = 2
fovCircle.Radius = aimRadius
fovCircle.Filled = false

local function IsVisible(targetPart)
    local char = LP.Character
    if not char then return false end
    local rayParams = RaycastParams.new()
    rayParams.FilterType = Enum.RaycastFilterType.Blacklist
    rayParams.FilterDescendantsInstances = {char, camera}
    local rayResult = game.Workspace:Raycast(camera.CFrame.Position, (targetPart.Position - camera.CFrame.Position).Unit * 1000, rayParams)
    return rayResult == nil or rayResult.Instance:IsDescendantOf(targetPart.Parent)
end

local function UpdateESP()
    for _, v in pairs(Players:GetPlayers()) do
        if v.Character then
            local hl = v.Character:FindFirstChild("Highlight") or Instance.new("Highlight", v.Character)
            hl.Enabled = Settings.WallHack
            hl.FillColor = Color3.fromRGB(255, 0, 0)
        end
    end
end

UIS.JumpRequest:Connect(function()
    if Settings.InfJump then LP.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping") end
end)

local function GetTarget()
    if Settings.CurrentTarget and Settings.CurrentTarget:FindFirstChild("Humanoid") and Settings.CurrentTarget.Humanoid.Health > 0 then
        if Settings.WallCheck and IsVisible(Settings.CurrentTarget[Settings.LockPart]) then return Settings.CurrentTarget
        elseif not Settings.WallCheck then return Settings.CurrentTarget end
    end
    Settings.CurrentTarget = nil
    local target = nil
    local dist = math.huge
    local mouse = LP:GetMouse()
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LP and v.Character and v.Character:FindFirstChild(Settings.LockPart) then
            if Settings.TeamCheck and v.Team == LP.Team then continue end
            local hum = v.Character:FindFirstChild("Humanoid")
            if hum and hum.Health > 0 then
                local part = v.Character[Settings.LockPart]
                local pos, onScreen = camera:WorldToViewportPoint(part.Position)
                local visible = Settings.WallCheck and IsVisible(part) or not Settings.WallCheck
                if onScreen and visible then
                    local mDist = (Vector2.new(pos.X, pos.Y) - Vector2.new(mouse.X, mouse.Y)).Magnitude
                    if mDist < dist and mDist < aimRadius then dist = mDist; target = v.Character end
                end
            end
        end
    end
    Settings.CurrentTarget = target
    return target
end

RunService.RenderStepped:Connect(function()
    fovCircle.Position = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
    if Settings.Aimbot then
        local t = GetTarget()
        if t then camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, t[Settings.LockPart].Position), Settings.Smoothness) end
    elseif Settings.Camlock then
        local t = GetTarget()
        if t then camera.CFrame = CFrame.new(camera.CFrame.Position, t[Settings.LockPart].Position) end
    end
end)

local Window = WindUI:CreateWindow({Title = "ART HUB", Icon = LOGO_ID, Size = UDim2.fromOffset(600, 450)})
local T_Combat = Window:Tab({Title = "Combat", Icon = "target"})
local T_Scripts = Window:Tab({Title = "Scripts", Icon = "code"})

T_Combat:Toggle({Title = "Aimbot", Callback = function(v) Settings.Aimbot = v end})
T_Combat:Toggle({Title = "Camlock", Callback = function(v) Settings.Camlock = v; fovCircle.Visible = v end})
T_Combat:Toggle({Title = "Team Check", Value = true, Callback = function(v) Settings.TeamCheck = v end})
T_Combat:Toggle({Title = "Wall Check", Value = true, Callback = function(v) Settings.WallCheck = v end})

T_Scripts:Toggle({Title = "WallHack (ESP)", Callback = function(v) Settings.WallHack = v; UpdateESP() end})
T_Scripts:Toggle({Title = "Infinite Jump", Callback = function(v) Settings.InfJump = v end})
T_Scripts:Toggle({Title = "Noclip", Callback = function(v) Settings.Noclip = v end})
T_Scripts:Button({Title = "Enable FLY", Callback = function() loadstring("\108\111\97\100\115\116\114\105\110\103\40\103\97\109\101\58\72\116\116\112\71\101\116\40\40\39\104\116\116\112\115\58\47\47\103\105\115\116\46\103\105\116\104\117\98\117\115\101\114\99\111\110\116\101\110\116\46\99\111\109\47\109\101\111\122\111\110\101\89\84\47\98\102\48\51\55\100\102\102\57\102\48\97\55\48\48\49\55\51\48\52\100\100\100\54\55\102\100\99\100\51\55\48\47\114\97\119\47\101\49\52\101\55\52\102\52\50\53\98\48\54\48\100\102\53\50\51\51\52\51\99\102\51\48\98\55\56\55\48\55\52\101\98\51\99\53\100\50\47\97\114\99\101\117\115\37\50\53\50\48\120\37\50\53\50\48\102\108\121\37\50\53\50\48\50\37\50\53\50\48\111\98\102\108\117\99\97\116\111\114\39\41\44\116\114\117\101\41\41\40\41\10\10")() end})
